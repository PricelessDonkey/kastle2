#pragma once
// Host-build stub. common/config.hpp includes "pico/stdlib.h" but doesn't
// actually use anything from it directly — it's dragged in for the RP2040
// build environment. Nothing to provide here.
